interface:
  display_name: "iOS Sim Serve"
  short_description: "Run mobile apps through serve-sim"
  default_prompt: "Use $ios-sim-serve to run this mobile app on the iOS Simulator and expose it in the browser."
